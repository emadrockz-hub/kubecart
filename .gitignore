# -----------------------------
# KubeCart - Git Hygiene
# -----------------------------

# Local-only Kubernetes secrets (do not commit real credentials)
k8s/secrets/*-secrets.yaml

# K8s real secrets (never commit)
kubecart/k8s/secrets/*-secrets.yaml
kubecart/k8s/secrets/*.local.yaml
kubecart/k8s/secrets/kubecart-secrets.yaml


# -----------------------------
# .NET / Visual Studio
# -----------------------------
**/bin/
**/obj/
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
*.cache
*.pdb
*.Backup.tmp

# -----------------------------
# Node / React / Vite
# -----------------------------
**/node_modules/
**/dist/
**/.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# -----------------------------
# Environment files (keep examples)
# -----------------------------
.env
.env.*
!.env.example

# -----------------------------
# OS / Editor junk
# -----------------------------
Thumbs.db
.DS_Store

# Local env (never commit)
.env.local
.env
